#include <iostream>   // for std::cout, stdio
using namespace std; // for standard io 
#include <string>    // for rfind or find
#include <regex>      // for std::regex_replace
#include <math.h> // for round()
#include <sstream> // for std::istringstream
#include <vector> // for std::vector<std::string>


constexpr size_t Hash(const char* str);
std::vector<std::string> split(const std::string &s, char delim);
std::string sutron(std::string obj);
